<IfModule mod_rewrite.c>
RewriteEngine On

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# Redirect old body-therapy URL
RewriteRule ^body-therapy/?$ https://mindbodycacao.com/somatic-therapy-mexico-city [R=301,L]

</IfModule>